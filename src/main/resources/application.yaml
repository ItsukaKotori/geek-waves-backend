server:
  port: 8082
spring:
  application:
    name: geekwaves-server
  task:
    scheduling:
      pool:
        size: 4   # 默认单线程会被监控采样(OSHI 进程枚举偶发数百 ms)阻塞抓取调度
  datasource:
    url: jdbc:h2:file:./data/geekwaves;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: false   # 调试用,默认关闭(README 口径一致);临时启用 --spring.h2.console.enabled=true
      path: /h2-console
  flyway:
    locations: classpath:db/migration/h2
itsuka:
  web:
    logging:
      enabled: true
geekwaves:
  storage:
    cache: local
  crypto:
    key: ${GEEKWAVES_CRYPTO_KEY:}   # 必填,空/缺失时 CryptoProperties 拒绝启动;不内置默认密钥
  monitor:
    enabled: true
logging:
  level:
    com.geekwaves: debug
